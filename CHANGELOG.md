# 维护记录

## v0.2.4

- 修复无时区报价时间在 UTC CI runner 上被误判为过期的问题；仍保留同日、未来偏差和 20 分钟新鲜度门禁。
- 增加带时区报价的回归覆盖，避免不同部署时区让风控读数出现环境差异。

## v0.2.3

### 调度与审计

- 一键启动和 Compose 默认启用内置 3 分钟盘中调度，独立克隆无需额外创建系统计划任务即可运行盘中监控与风控扫描。
- 增加 `-NoScheduler` / `--no-scheduler` 开关，外部调度器接管时可关闭内置线程，避免重复抢占。
- 调度状态接口区分系统任务是否安装与内置调度是否生效；风控审计页面展示最近扫描状态，即使当前无持仓也能确认扫描已完成。

## v0.2.2

### 启动与运行

- 增加 Windows 与 Linux/macOS 跨平台一键启动脚本，自动选择 Docker 或本地虚拟环境。
- 增加 Docker 不可用时的本地回退、依赖哈希复用、健康检查和可选浏览器启动。
- 公开仓库的启动脚本默认启用内置 3 分钟盘中调度，外部调度场景可显式关闭。
- README 补充从 clone、安装到启动看板的快速路径，并说明空账本与本地运行数据边界。

### 验证边界

- 本地后端单元测试 123 个通过，前端 JavaScript 与 PowerShell 启动脚本完成语法检查。
- 当前维护环境未安装 Docker，未执行实际镜像构建和容器启动验证。
- GitHub Actions 的远程运行仍以仓库页面结果为准。

## v0.2.1

### 策略与风控

- 公开模拟盘收敛为 `tq_breakout` 与 `main_force_top10` 两套策略，旧策略与历史账本不进入公开运行面。
- 买入与卖出沿用共享资金池、席位上限、T+1、价格有效性和风险退出门禁；两套策略分别记账、分别审批。
- 风控审计读取改为轻量只读模型，保留行情核验、系统动作、规则版本和关联信号等追溯字段。
- 前端审计请求条数与后端回归边界统一为 160 条，避免页面与接口出现截断差异。

## v0.2.0

### 首次公开维护

- 增加 Dockerfile、Compose 编排、非 root 容器用户、健康检查和持久化命名卷。
- Compose 使用仓库专用数据与报告卷，公开克隆从空账本开始，不连接其他实例的运行数据。
- 增加 MIT 开源许可证、GitHub Actions 自动测试、贡献指南和公开 Demo 状态说明。
- README 增加 Docker、本地运行、数据初始化、风控边界与部署说明。

### 脱敏与验证

- 公开提交仅包含源码、Docker 配置、测试和文档；不包含本机绝对路径、服务器地址/账号/密码、运行时数据库、日志、API key 或其他凭据。
- `DEEPSEEK_API_KEY` 仅以 `.env.example` 注释占位符形式保留。
- 从 GitHub 全新克隆后完成本地启动、首页与 `/api/health` HTTP 200 验证；Docker 仍需在安装 Docker 的环境中单独构建。

## v0.1.0

- 初始公开版本：A 股研究、模拟撮合、双账本风控审计、静态 Web 看板和离线测试基线。
