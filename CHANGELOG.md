# 版本更新记录

本文按 Git tag 记录公开仓库的可复现变化。Release 页面只保留版本号，详细变更、运行边界和验证结果统一维护在这里。

## Unreleased

### 代码审计与策略兼容性

- 保留 `strategy_registry` 中原有五套策略及自适应、研究、回放查询范围；本轮没有删除、重命名或改写策略规则。
- 为回测评分与市场门控序列增加有界缓存，避免长驻服务按日期和参数组合无限保留大型对象。
- 移除已确认未使用的 Python 导入，并将可选 ModLens 依赖改为调用时加载，减少启动副作用。
- 修复前端重复的个股分析函数；旧实现改为显式兼容入口，避免同名函数覆盖。
- 市场门控徽标在首页启动、手动刷新和三分钟轮询时同步更新；旧页面片段缺失时安全跳过，不再产生空节点异常。
- 个股搜索、同行业列表和研究结果对动态名称/代码进行转义与参数编码，降低外部行情文本注入风险。

### 架构链路与运行时修复

- 修复 `trend_continuation` 自适应条件读取未定义 `_num` 导致的运行时 `NameError`，统一使用既有数值转换函数。
- 以 `strategy_registry.active_ids()` 作为新周期唯一策略范围：新建/重置周期、资金分摊、账户状态、信号生成、日内风控、Dashboard 和风险中心只处理 `tq_breakout` 与 `main_force_top10`；旧策略规格仍保留给历史审计与回放，不再获得新周期资金或调度时间。
- 为 `main_force_top10` 补齐自适应风控与下行保护基线，避免风控进化读取活动账户时发生 `KeyError`。
- 纯交易规则模块改为按需导入行情/日历模块，减少启动时的 pandas、请求库和缓存目录副作用；健康响应移除重复 `warnings` 字段。

### 前端可用性与资源占用

- 补齐数据有效性中心的“重建因子”“取消增量”操作及状态轮询；轮询复用同一次响应，不再每轮重复请求 `/api/data-validity`。
- 恢复板块、回测、优化、个股研究的主导航入口；顶部个股搜索不再被全局 CSS 隐藏，搜索跳转与 `p-stock` 页面链路可用。
- 默认加载仓库内置 ECharts，离线或静态资源尚未就绪时图表使用无操作降级适配器，表格与风控功能仍可用。
- 为行情、替代数据、模拟盘和自进化读取缓存增加进程内条目上限，防止按代码/日期增长的键长期占用内存。

### 本次验证

- `.venv` 下 `pytest` 206 项全部通过（使用仓库内独立 `--basetemp` 避免 Windows 临时目录权限差异）。
- `unittest discover` 199 项全部通过；后端 `compileall`、前端两个主脚本 `node --check` 均通过。
- Docker 仍需在安装 Docker 的环境执行真实构建/容器启动验证；本维护环境未宣称已完成 Docker 运行验证。

### 开源维护与可发现性

- 重构中文 README 首页，先说明项目定位、A 股执行约束、数据质量门禁、审计回放和并发安全，再进入安装与运行步骤。
- 增加英文版 `README_EN.md`，覆盖项目边界、两套公开策略、运行方式、风险声明和贡献入口。
- 增加 `MAINTAINERS.md`、Issue 模板、Feature Request 模板和 Pull Request 模板，统一复现信息、验证要求和公开贡献流程。
- 补充公开 roadmap、贡献指南、CI/License/Python 状态徽章和 Dashboard 截图。
- 将 GitHub 仓库简介与 Topics 对齐到当前实现：A 股、量化研究、模拟盘、T+1 执行、风控审计和 Docker 运行；不再使用“5 strategies”或“T+0”表述。

### 首次运行体验

- 修复全新空账本首次访问模拟盘页面时 `paper_jobs` 尚未建表导致的 500；调度状态读取现在会先完成公开克隆所需的数据库初始化。
- Dashboard 截图来自空账本公开克隆，不包含本地运行数据库、服务器配置、凭据或历史交易记录。

### 当前验证边界

- 本地后端 123 个 `unittest` 用例通过，Python 编译、前端 JavaScript 语法和 PowerShell 启动脚本解析通过。
- GitHub Actions 已在 Python 3.11 和 3.12 矩阵通过。
- 当前维护环境未安装 Docker，Docker 镜像构建与容器启动仍需在安装 Docker 的环境中验证。

## v0.2.3 — 独立克隆内置调度与风控审计可见性

### 调度行为

- 一键启动脚本和 Compose 默认设置 `ASTOCK_ENABLE_FALLBACK_THREADS=1`，启动后自动拉起内置盘中线程。
- 内置线程按交易日和交易时段每 3 分钟触发 `intraday` slot：上午 09:30–11:25，下午 13:00–14:55；每轮统一经过租约、幂等键和风险监控路径。
- 内置调度只负责盘中监控与风控扫描，不自动创建新的模拟交易周期，也不绕过数据覆盖率、行情新鲜度或下单风控门禁。
- 已配置完整宿主机计划任务时，可使用 `-NoScheduler` / `--no-scheduler` 或将环境变量设为 `0`，避免外部任务与进程内线程重复抢占。

### 状态与审计

- `schedule_status()` 新增 `fallback_enabled` 与 `effective` 字段，区分“操作系统计划任务已安装”和“独立克隆实际具备可运行调度”。
- 当内置调度已开启但尚未到交易时段，状态显示“内置调度已启用，等待交易时段”，不再误报为未安装。
- 风控审计接口返回最近 `risk_scan_state` 扫描标记；即使当前没有持仓、没有个股决策，也能显示扫描是已完成、执行中还是失败。
- 前端风控审计区增加最近扫描状态提示，避免把“扫描完成但无对象可处置”误解为“风控没有运行”。

### 运行文件

- Compose 增加 `ASTOCK_ENABLE_FALLBACK_THREADS` 配置，默认值为 `1`，外部调度部署可显式覆盖为 `0`。
- 保持数据卷按仓库实例隔离，从空账本启动；运行时数据库、报告和日志不进入 Git。

## v0.2.2 — 跨平台一键启动

### 启动方式

- 增加 Windows `start.bat` / `start.ps1`，以及 Linux/macOS `start.sh`。
- 默认优先尝试 Docker Compose；Docker 不可用或启动失败时，在未强制 Docker 的情况下回退到本地 Python 虚拟环境。
- 本地模式首次运行自动创建 `.venv`，按照 `requirements.txt` 的 SHA-256 哈希决定是否安装或更新依赖。
- 启动后轮询 `/api/health`，服务就绪后可自动打开 Dashboard，也可通过 `-NoBrowser` / `--no-browser` 关闭浏览器启动。
- 支持 `-Local`、`-Docker`、自定义端口和跳过依赖安装；Docker 模式固定使用 Compose 暴露的 8600 端口。

### 数据与文档

- README 增加从 clone、依赖安装、看板启动到手动 slot 扫描的快速路径。
- 明确 `.env`、`.venv`、`data_cache/` 和 `reports/` 只保留在运行机器；公开仓库不携带模拟成交、持仓、审计或自进化历史。
- Compose 使用仓库专用命名卷，避免新克隆误接入其他实例的数据卷。

### 验证边界

- 本地后端 123 个测试通过，首页和 `/api/health` 已完成 HTTP 200 验证。
- PowerShell 脚本完成语法解析；当前维护环境没有 Docker，因此未宣称镜像构建通过。

## v0.2.1 — 执行门禁与风控策略收紧

### 资金、席位与下单

- 最小建仓金额从 8,000 元提高到 10,000 元；能买出一手但金额不足的候选进入等待/容量重排，不再生成尘埃仓。
- 为 `main_force_top10` 保留 15% 策略最低资金份额；该保护只抬高策略自身可表达额度，不突破共享资金池硬上限。
- 共享资金池继续执行总敞口、动态预算、席位、单票和行业上限；两套策略分别决策、分别审计。
- 回测佣金调整为万一、免最低佣金，与模拟盘费用模型保持一致；卖出印花税和滑点仍单独计入。

### 入场与退出风控

- 入场时机状态机保留重触发序号和 lineage，前端与日志可区分首次触发、超时后重触发和连续确认阶段。
- 开盘执行前强制探活数据源并刷新入场冻结状态；探活失败继续 fail-closed，不会放宽买入条件。
- 下行保护的 giveback 确认在当日已触发后保持有效，避免短暂反弹让保护性退出重新变得不可达。
- 主力资金策略趋势结构增加可配置的 MA20/MA60 最小差值；底部反转候选要求 RSI 数据有效，缺数据不再伪装成有效信号。

### 数据与前端

- 北向资金缓存增加交易日键，跨午夜不会继续读取上一交易日累计值。
- 行情、风险决策、容量等待、T+1 状态和前端展示字段继续保持结构化，便于审计回放与回归测试。
- 风控审计请求条数与后端回归边界统一为 160，修复页面截断导致的“记录缺失”错觉。

## v0.2.0 — 双策略公开版与 Docker 基线

### 公开模拟盘范围

- 公开新周期固定启用两套策略账户：`tq_breakout`（强势突破）和 `main_force_top10`（主力资金跟随）。
- 旧策略和历史账本可以保留用于回放，但不进入新周期的正式调度、资金分配或候选池。
- 两套策略共用撮合、资金池、风控和审计基础设施，同时保留独立候选车道、参数版本、席位和绩效归因。

### A 股执行约束

- 股票 T+1、100 股整手、涨跌停、停牌、佣金、印花税、滑点和可卖数量都在下单路径校验。
- 新买入默认要求当日实时行情与独立来源交叉核验；陈旧、缺失、异常或覆盖率不足的行情进入等待或拒绝，不以缓存旧价伪装成交。
- 共享资金池增加总敞口、单票、行业和席位硬上限；待成交买单会预占现金与额度，撤单/过期/拒绝后释放。
- 风险决策、信号、委托、成交、持仓、NAV 和扫描结果形成可回放台账；研究/自适应模块只写影子证据，不绕过正式撮合。

### Docker 与公开运行

- 增加 Dockerfile 和 `docker-compose.yml`，使用非 root 用户运行 FastAPI/Web 看板。
- 增加健康检查、`PYTHONDONTWRITEBYTECODE`、时区配置和数据/报告命名卷；镜像本身不携带部署侧 cron、服务器配置或历史账本。
- README 与 `docs/RUNBOOK.md` 补充本地运行、Compose 启停、数据初始化、手动 slot 和风控边界说明。

### 开源与验证

- 增加 MIT License、GitHub Actions 测试工作流、贡献指南和公开 Demo 状态说明。
- `.env.example` 只保留注释占位符；公开提交不包含本机绝对路径、服务器地址、账号密码、运行时数据库、日志或真实 API key。
- Docker 构建需要在安装 Docker 的环境中执行；本地测试与接口验证不能替代真实容器运行验证。

## v0.1.0 — 初始公开基线

- 建立 A 股量化研究与模拟撮合的公开代码结构。
- 提供静态 Web 看板、选股/回测入口、模拟账户、风险决策和审计台账基础能力。
- 建立离线单元测试和“源码、配置、运行数据分离”的公开仓库边界。
