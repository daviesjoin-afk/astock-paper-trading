# 在线 Demo / 截图说明

> 状态：当前没有托管的在线 Demo。

本页是公开仓库的演示信息占位。当前仓库提供可本地运行的源码、Docker 配置和研究说明，但没有公开可访问的部署实例。因此本页不提供在线访问入口，也不声称存在已经上线的 Demo。

## 当前状态

- 托管 Demo：暂无。
- 截图：README 已收录一张 Dashboard 预览（`docs/assets/dashboard.png`），来自全新公开克隆的空账本，展示可视化看板、委托记录和风控审计入口；不包含任何真实运行数据。
- 公开运行数据：暂无。仓库不包含真实账户、持仓、订单、成交、行情快照或运行日志。

## 截图

现有截图只展示空账本下的只读界面，任何后续新增截图都应只使用合成数据或经过审核的脱敏数据，并在说明中标注数据来源。当前不放置未审核的图片、外部链接或运行实例信息。

> **已知状态**：仓库当前这张 `docs/assets/dashboard.png` 拍摄于**策略工坊上线之前**，因此没有展示
> Strategy Workbench 与策略平台相关界面。仓库目前**没有**脚本化的截图流水线（Playwright 只在用例失败时
> 保留截图/录像），所以本轮不重新生成截图，以免引入未经审核或不可复现的图片；如需更新，请先补一个
> 基于合成数据（`ASTOCK_DEMO=1`）的可复现截图步骤，再替换图片并标注数据来源。

## 发布前检查

添加在线 Demo 或截图前，需要确认：

- 页面和图片中的数据均为合成数据，或已获得公开使用许可并完成脱敏；
- 已移除本机路径、服务器域名/IP/端口、账号、密码、令牌、API key 及其他凭据；
- 不展示真实用户、持仓、订单、成交、日志或缓存内容；
- 文案准确标注 Demo 的部署状态，不把本地运行或截图描述为已托管服务；
- 不将模拟盘结果表述为收益保证或投资建议。
